================================================================================
HEROES OF LUMORA - CHANGE LOG
================================================================================

--------------------------------------------------------------------------------
SESSION: 2026-05-28
--------------------------------------------------------------------------------

BUG FIX: Annadaeus (Troubadour) not firing Symphony of Fate bolts at enemies
  Files: Scripts/annadaeus.gd, Scenes/annadaeus.tscn
  Problem: _has_clear_line_to_target() was checking the avoidance_ray before
    updating its direction to point at the target. The ray used its previous
    frame direction (often pointing at the player or a wall), causing the
    line-of-sight check to always fail and the troubadour_bolt to never fire.
    Additionally, the avoidance_ray collision_mask included the player layer (1)
    and friendly NPC layer (32), so the player standing between Anna and a
    monster always blocked the check.
  Fix: Added _update_avoidance_ray(target, support_range) call inside
    _has_clear_line_to_target() before the collision check. Changed
    avoidance_ray collision_mask in annadaeus.tscn from 1075 to 2066
    (monsters + environment + walls only, removes player and friendlies).

BUG FIX: Priestess heal bolt passing through some friendly targets
  File: Scripts/healing_projectile.gd
  Problem: despawn() was nested inside the inner `if body.has_method("heal")`
    block. Friendly/player bodies without a heal method were detected but the
    bolt would not despawn, silently flying through them.
  Fix: Moved despawn() outside the inner conditional so it always fires when
    a friendly or player body is touched, regardless of heal method presence.

BUG FIX: Tenchijin health bar not visible
  File: Scenes/tenchijin.tscn
  Problem: tenchijin.gd._ready() adds a _name_label dynamically at y=-38 to
    y=-20. The health bar was at offset_top=-32, offset_bottom=-30, placing it
    directly behind the name label.
  Fix: Moved health bar offsets to top=-44, bottom=-42 (above the name label),
    matching the same fix previously applied to Annadaeus.

BUG FIX: Mh'orzath becoming invisible during boss fight
  File: Scripts/monsters.gd
  Problem: _damage_flash() set sprite.modulate = Color.BLACK. The game
    background is black, so a black modulate made the boss invisible. With
    rapid player hits, multiple concurrent await coroutines stacked, each
    trying to reset the modulate to WHITE after 0.05s, but immediately
    overridden by the next hit setting it back to BLACK. The boss stayed
    permanently invisible while alive.
  Fix: Changed flash color from Color.BLACK to Color.RED. Added _is_flashing
    bool guard to prevent coroutine stacking. Added _is_flashing = false to
    reset() so pooled monsters start clean.

BUG FIX: Wall of Heroes always showing "slain by a projectile" instead of
    custom epitaphs
  Files: Scripts/skeleton.gd, goblin.gd, wizard.gd, wraith.gd, troll.gd,
         ghost.gd, balrog.gd, beholder.gd, lich.gd, hezrou.gd, ogre.gd
  Problem: Every monster's _cast() override set owner_group = "monsters" on
    the projectile but never set shooter = self. When the player died,
    player.gd._identify_killer() called source.get("shooter") which returned
    null, so it fell back to the projectile node itself as the attacker.
    The projectile script basename is "projectile", so
    Global.generate_epitaph("projectile") hit the fallback branch and
    returned "slain by a projectile" for every death.
  Fix: Added `<projectile_var>.shooter = self` as the first line inside each
    monster's if-projectile-spawned block. Variable names per monster:
      skeleton -> bone
      goblin   -> dagger
      wizard   -> fireball
      wraith   -> lifedrain
      troll    -> poison_bolt
      ghost    -> ghost_fire
      balrog   -> malfire
      beholder -> disintegration_ray
      lich     -> death_bolt
      hezrou   -> bile_spew
      ogre     -> boulder
  Note: monsters.gd base _cast() already set shooter = self correctly. Only
    the per-monster overrides were missing it.

BUG FIX: Mh'orzath running away to map edge mid-fight
  File: Scripts/mh_orzath.gd
  Problem: The boss inherits monsters.gd's waypoint movement system. On spawn
    it initializes at waypoint_stage=0 (move to village center), then advances
    to stage 1 (move to extraction point at map edge), and stops there. The
    boss's ranged abilities kept firing from the edge, making the fight
    unplayable. The boss "came back" only in the sense that it stopped moving
    at stage 2 (arrived at extraction point) while still shooting.
  Fix: Added _initialize_waypoint() override in mh_orzath.gd that immediately
    sets waypoint_stage = 2. The boss skips all waypoint movement entirely and
    stays in its spawn position, relying on its abilities (Event Horizon,
    Nihil Storm, and ranged projectiles) to engage the player.

BUG FIX: Area outside grass tilemap visually black but still traversable
  File: Scripts/main.gd
  Problem 1 (wrong bounds): _setup_map_bounds() merged the used_rect from all
    four TileMapLayers (grass, roads, town, walls). If the walls or town layers
    had decorative tiles placed outside the grass area (to create the black
    visual border), the merged bounds would be enormous, placing the invisible
    StaticBody2D walls far from the actual playable area. Players and monsters
    could freely walk over the black tile region.
  Problem 2 (monsters not blocked): The boundary StaticBody2D walls used
    collision_layer = 16 (Environment, layer 5). Monster collision_mask does
    not include layer 5, only layer 12 (Walls, 2048). So monsters including
    the boss were not stopped by the walls at all.
  Fix 1: Changed _setup_map_bounds() to read only the grass TileMapLayer's
    used_rect. The grass layer defines the visual playable area; other layers
    should not expand the bounds.
  Fix 2: Changed boundary wall collision_layer from 16 to 16 + 2048
    (Environment + Walls). Player mask includes both (16+2048). Monster mask
    includes 2048. Both are now blocked at the map edge.

--------------------------------------------------------------------------------
SESSION: 2026-07-18
--------------------------------------------------------------------------------

BUG FIX: Game crashed on startup (Variant type inference in pet.gd)
  File: Scripts/pet.gd
  Problem: `var last := _last_damage_times.get(body, 0.0)` — Dictionary.get()
    returns Variant, and the project treats warnings as errors, so the
    inferred-Variant warning was fatal at parse time.
  Fix: Explicit `var last: float = _last_damage_times.get(body, 0.0)`.

FEATURE: Villagers and guards wobble while moving
  Files: Scripts/villager.gd, Scripts/guard.gd
  Added _move_wobble() (sine-wave sprite rotation keyed off velocity),
  matching the wobble already used elsewhere, called each _process().

FEATURE: Pet companion can be healed/buffed, is non-killable, and has a
    boss-aware leash range
  File: Scripts/pet.gd
  - Added heal(), get_health(), get_max_health(), set_damage_modifier() so
    Annadaeus's auras and priestess heal bolts can affect the pet.
  - take_damage() now floors health at 1 instead of dying; removed the old
    _is_dead / _die() / _respawn() flow entirely (dead code once the pet
    can no longer die).
  - _LEASH_RANGE raised from 256 to 480 (15 tiles) so the pet actually
    engages enemies near the player instead of ignoring most of the map.
  - Added _is_within_leash() with a boss-aware exception: 900px leash when
    the target is in the "final_boss" group, since boss-to-player distance
    routinely exceeds a normal monster's engagement range.

BUG FIX: Second crash after the leash-range fix ("Cannot infer the type of
    'dist' variable")
  File: Scripts/pet.gd
  Problem: `var dist := _player.global_position.distance_to(mob.global_position)`
    — _player and mob are typed as generic Node, so .global_position
    resolves to Variant.
  Fix: Explicit `var dist: float = ...` in _is_within_leash().

FEATURE: Death epitaphs and victory sayings now name the specific monster
    and weapon, with a random line per combination
  Files: Scripts/global.gd, Scripts/player.gd, Scripts/game_over.gd,
         Scripts/victory_scene.gd
  - generate_epitaph(killer, weapon) rewritten around {melee: [...],
    ranged: [...]} string buckets per monster, weapon name substituted via
    %s where the bucket is "ranged" (e.g. "the wizard's fireball").
  - _identify_killer() in player.gd now returns {monster, weapon}, deriving
    the weapon name from the projectile's scene_file_path — no per-monster
    script changes needed.
  - Added a new victory-sayings bucket (referencing the player's initials
    and Mh'orzath by name) for the Wall of Heroes after a win.
  - Removed the duplicate boss-spawn system: global.gd had its own
    _start_boss_sequence()/_spawn_boss()/_on_boss_died() firing on the same
    "50 villagers saved" event that final_boss_spawner.gd already handles,
    so two Mh'orzath instances were being spawned. final_boss_spawner.gd is
    now the sole trigger.

BUG FIX: Barrage arrow spread too wide, missing enemies standing close
    together
  File: Scripts/player.gd
  Problem: Fixed 40-degree total cone regardless of arrow count meant the
    outer arrows in a 5-arrow Barrage diverged too far to hit anything.
  Fix: Fixed 5-degree spacing between adjacent arrows instead of a fixed
    total spread, so the cone scales sensibly with arrow count.

BUG FIX: Tenchijin retreating away from the player into monsters and dying
  File: Scripts/tenchijin.gd
  Problem: A STRATEGIC_MOVE state and _strategic_target var existed but
    _strategic_target was never assigned, so entering that state made Ten
    beeline for world origin (0,0) whenever he lost a target — frequently
    running through enemy monsters on the way.
  Fix: Removed the dead STRATEGIC_MOVE state and _strategic_target var
    entirely.

BUG FIX: Anna and Tenchijin wandering off to seek a healer mid-boss-fight
  Files: Scripts/annadaeus.gd, Scripts/tenchijin.gd
  Fix: Added `and not Global.boss_fight_active` to the seek-healer trigger
    in both _update_targets()/_update_target() — they're needed at the
    player's side during the boss fight, not making a trip back to town.

BUG FIX: Mh'orzath (final boss) frozen in place for the entire fight
  File: Scripts/mh_orzath.gd
  Problem: monsters.gd's base _physics_process() hard-freezes movement once
    waypoint_stage reaches 2 ("arrived at hunting grounds, just shoot in
    place") — correct for regular monsters, fatal for a boss that needs to
    chase a moving player.
  Fix: Full _physics_process() override: chases the player whenever
    target_distance > shoot_range, otherwise drifts to a stop. Also
    overrode _initialize_waypoint() to force waypoint_stage = 2 immediately
    on spawn (skip the move-to-village-center/move-to-extraction-point
    walk entirely) and _has_line_of_sight() to unconditionally return true
    (the shared avoidance_ray check breaks against his own oversized
    hitbox — see hitbox fix below).

BUG FIX: Mh'orzath trivially easy to kill by kiting at range; never
    actually threatened the player
  File: Scripts/mh_orzath.gd
  - max_speed raised 65 -> 120 (faster than the player's 100 base speed,
    so sustained kiting no longer works).
  - _find_nearest_attack_target() overridden to always lock onto the
    player specifically — the inherited version picked whichever of
    player/friendly/villagers was globally nearest, so he'd wander off
    after a guard standing near his spawn point instead of chasing.
  - collision_mask &= ~32 (drop the friendly-NPC layer) so he no longer
    physically drags/shoves Tenchijin and Annadaeus around while pursuing
    the player — his 120 speed vs. their lower speeds meant they could
    never break free once snagged.

BUG FIX: Anna and the pet not engaging Mh'orzath during the boss fight
  Files: Scripts/annadaeus.gd, Scripts/pet.gd
  Problem: Both used engagement ranges tuned for regular monsters (300px /
    480px), while boss-to-player distance routinely ran 450-780px in
    actual play.
  Fix: Added boss-aware 900px engagement ranges (effective_range in
    _find_closest_mob() for Anna, _is_within_leash() for the pet), and
    bypassed the LOS raycast check for final_boss-group targets in both
    (same oversized-hitbox raycast problem as mh_orzath's own LOS check).

BUG FIX: Enemies occasionally taking continuous damage at their spawn
    point with no attacker nearby
  File: Scripts/monsters.gd
  Problem: reset() (called when a pooled monster is reused) didn't clean
    up leftover Timer children from the previous occupant's DoT/mark/
    root/slow status effects. Those timers kept ticking on the invisible
    "dead" node and fired against whatever monster the pool handed the
    node to next.
  Fix: reset() now stops and frees any Timer children before reuse.

BUG FIX: Mh'orzath's abyss touch (ranged bolt) never fires
  File: Scripts/mh_orzath.gd
  Problem 1: shoot_range was 350, well under his own collision hitbox's
    effective reach at the time (~660px — see hitbox fix below), so by the
    time he was close enough to physically touch the player he was already
    outside the range gate that permits firing. Raised shoot_range to 500
    to match event_horizon_range.
  Problem 2 (the real blocker): see the hitbox fix below — even after
    raising shoot_range, he still couldn't get under it without already
    colliding, because his hitbox was ~2.5x too large.

BUG FIX: Mh'orzath appears to "chase then get stuck to the player," root-
    locked in place for large stretches of the fight
  File: Scripts/monsters.gd
  Problem: apply_root() had no boss exception. The player's Ensnaring Trap
    (5s cooldown, no range check — always targets the nearest enemy
    unconditionally) roots whatever it hits for 2s. With the boss as the
    only enemy on the field for the whole fight, that's a 2-of-every-5-
    second freeze for the entire encounter — reads exactly like "stuck to
    me." Confirmed via targeted debug logging: every freeze was
    immediately preceded by an apply_root() call, no exceptions either
    way.
  Fix: apply_root() now returns immediately (no-op) for anything in the
    "final_boss" group, so neither Ensnaring Trap nor Tenchijin's Frost
    Nova can lock him in place anymore.

BUG FIX: Mh'orzath's collision hitbox roughly 2.5x larger than his visible
    sprite in every direction
  File: Scenes/mh_orzath.tscn
  Problem: His CollisionShape2D had scale ~1.0 (Vector2(0.99999976, ...)),
    apparently intended to cancel out the root CharacterBody2D's 2.5x
    scale. A child node's own scale does not offset its parent's scale in
    Godot — they multiply — so the shape's true world-space radius was
    264 * 2.5 = 660px (diameter ~1320px) instead of the intended 264px.
    This is what actually caused the "stuck to player from far away"
    reports: physical contact started at ~660-680px separation, which is
    outside 500px shoot_range, so he could never simultaneously be "in
    range" and "not yet touching" the player.
  Fix: Set the CollisionShape2D's local scale to 0.4 (1/2.5) so the
    composed world-space radius comes back to the authored 264px, matching
    his sprite size. Contact now starts around ~280-300px, safely inside
    shoot_range.

BUG FIX: Abyss touch bolt fires and travels correctly but is barely
    visible / never noticed landing
  File: Scenes/abyss_touch.tscn
  Problem: Confirmed via debug logging that _cast() fires on schedule, the
    projectile spawns, is visible, is correctly reparented under the main
    scene, and travels its full straight-line distance before its 4s
    destroy_timer expires — the firing pipeline itself was never broken.
    Its sprite scale (0.075) was half the wizard's fireball (0.15) for
    what's supposed to be the final boss's signature attack, easily lost
    against boss-fight VFX clutter.
  Fix: Increased sprite scale, projectile speed, and collision shape size
    (final values tuned directly by Ross in the editor after the initial
    pass) so the bolt reads clearly on screen and can actually connect.
  Note: Editing Sprite2D scale via the Inspector on this node triggered a
    reproducible Godot editor crash (abort inside TextureRect::set_texture
    -> CanvasItem::queue_redraw -> CallQueue, a stale-Callable bug in the
    editor's texture-preview redraw path — confirmed via coredump
    backtrace, zero GDScript frames involved). Editing the scale value
    directly in the .tscn text avoids triggering it; dragging/typing the
    value in the Inspector does not.

--------------------------------------------------------------------------------
SESSION: 2026-07-24
--------------------------------------------------------------------------------

FEATURE: Torches added flanking shop entrance to improve discoverability
  File: Scenes/main.tscn
  Added two torch instances (torch_shop_l, torch_shop_r) positioned at
  (125, 55) and (182, 55) — one on each side of the shop doorway collision
  shape. Uses the existing torch.tscn scene (animated flame + PointLight2D +
  audio), consistent with all other landmark torches in the world.

BALANCE: Camera zoom-out limited to prevent trivialising combat
  File: Scripts/camera_controller.gd
  Added MIN_USER_ZOOM (0.65) and MAX_USER_ZOOM (3.0) constants. _user_zoom
  is now clamped on both mouse-wheel and pinch-to-zoom input, so players
  cannot zoom out far enough to see and preempt the full enemy spawn field.

BALANCE: Enemy spawn rate increased by 150%
  File: Scripts/enemy_spawner.gd
  min_spawn_time reduced 3.0 → 1.2s, max_spawn_time reduced 5.0 → 2.0s
  (2.5× the original spawn frequency). Wave-over-wave escalation via
  spawn_rate_increase_per_wave is unchanged.

BALANCE: Shop upgrade prices switched from Fibonacci to gentle exponential
  File: Scripts/shop_ui.gd
  Replaced the _fib(n) multiplier (Fibonacci, ~1.618× per purchase) with
  pow(1.3, n) (~1.3× per purchase). First purchase is always the base price;
  each subsequent purchase costs 30% more than the last. Reduces late-game
  price spikes substantially while still discouraging infinite stacking.


The magi and guards were too inexpensive to buy, so players could easily overwhelm the enemies by buying many of them. Updated pricing to balance this.

● Simple fix — just bumping the base prices in shop_ui.gd.

● Update(Scripts/shop_ui.gd)
  ⎿  Added 2 lines, removed 2 lines
      20    "health": 20,
      21    "damage": 30,
      22    "speed": 25,
      23 -  "guard": 50,
      24 -  "magi": 75
      23 +  "guard": 100,
      24 +  "magi": 150
      25  }
      26
      27  const GUARD_SCENE: String = "res://Scenes/guard.tscn"

● Doubled both — guard goes 50 → 100, magi goes 75 → 150. Combined with the 1.3× exponential, a third guard now costs ~169 and a
  third magi ~254, so stacking gets expensive fast. Let me know how it feels in testing and we can adjust from there.

--------------------------------------------------------------------------------
SESSION: 2026-08-08
--------------------------------------------------------------------------------

FEATURE: Voidknight and Gravecaller added as elite enemies (non-endless mode)
  Files: Scripts/voidknight.gd, Scripts/gravecaller.gd, Scripts/enemy_pet.gd,
         Scenes/voidknight.tscn, Scenes/gravecaller.tscn, Scenes/life_siphon.tscn,
         Scripts/enemy_spawner.gd, Scripts/projectile.gd, Scripts/player.gd
  Two new boss-tier enemies, wired into enemy_spawner.gd's boss_configs at the
  same rarity as Lich/Hezrou/Balrog (min_wave 7 and 8 respectively). Both
  extend monsters.gd (not the friendly-NPC FSM pattern) so they get spawner
  pooling, take_damage, potion drops, and waypoint pathing for free. This is
  a trimmed elite-enemy subset of their full class-notes kit — the complete
  progression (Dark Sovereignty, Reaper's Plague, Circle of Blight, etc.) is
  endless-mode scope, for when these become playable classes.
  - Voidknight: melee-leaning tank. Life Siphon is a ranged bolt
    (life_siphon.tscn, fired through the standard bullet_pool/_cast() path)
    that heals Voidknight for 50% of damage dealt — added a generic
    lifesteal_percent field to projectile.gd for this (same pattern as the
    existing slow_percent/apply_dot_on_hit flags, so any future spell can
    opt into lifesteal by just setting a field on its projectile instance).
    Also has Necrotic Grasp (slow), Shadow Ward (self-shield, absorbs flat
    damage before HP), and Void Slash (AoE burst) — these run on their own
    cooldowns in an overridden _process(), independent of the Life Siphon
    cast gate, so they still fire at close range even between bolts.
    Permanent Skeleton minion.
  - Gravecaller: ranged DoT caster. Death Bolt reuses death_bolt.tscn
    (same bullet Lich uses) through the standard _cast() path. Curse of
    Withering applies a DoT to its target; Plague Cloud spawns a lingering
    AoE damage zone (built programmatically, no new scene needed — same
    "construct in code" pattern as ability_chest.gd). Permanent Wraith
    minion.
  - Added apply_slow(percent, duration) and apply_dot(damage_per_tick, ticks)
    to player.gd, mirroring the equivalent methods already on monsters.gd —
    needed so these two elites' debuffs have something to call on the player
    (previously only monsters could be slowed/DoT'd).
  Sprite regions for both classes plus their minions were measured directly
  from Assets/New Sprites/NPC Sprites 2.png's bounding boxes.

FEATURE: Pets (player's own + enemy minions) are now killable, with a 20s
    respawn — permanent death only once the owner is gone
  Files: Scripts/pet.gd, Scripts/enemy_pet.gd (new)
  Problem: the Woodstalker's Wolf/Bear/Panther pet (pet.gd) was invincible —
    take_damage() floored health at 1 instead of ever dying.
  Fix: pet.gd now dies for real and respawns near the player 20s later
    (permanently gone only if the player itself is gone by then).
    enemy_pet.gd is a new hostile mirror of the same pattern, used by
    Voidknight's Skeleton and Gravecaller's Wraith minions: same 20s
    respawn while their caster is alive, but the caster's mob_died signal
    calls notify_owner_died() on its minion, which makes any future kill
    permanent — no more respawning once the caster is dead.

FEATURE: Player's pet can pick up coins and potions
  File: Scripts/pet.gd
  Added a 45px Area2D pickup radius (same "loot" layer detection the player
  itself uses), listening for area_entered. Reuses coin.gd/potion.gd's
  existing collect() unchanged — potion.gd already resolves the player
  directly and applies its effect regardless of what triggered pickup, so
  no changes were needed there. The pickup collider disables/re-enables
  alongside the pet's combat collider on death/respawn so a dead pet lying
  on the ground doesn't vacuum up loot.

BUG FIX: Monsters camping near a town-edge extraction point would target
    and cast at the priestess, who's supposed to be immune
  File: Scenes/priestess.tscn
  Problem: monsters.gd's _find_nearest_attack_target() explicitly excludes
    healers/priestesses — healer.gd correctly only adds the priestess to
    the "healing_npcs"/"healer" groups, never "friendly". But priestess.tscn
    has a leftover Area2D/defense_zone child node — inherited from being
    cloned off guard.tscn's template — still tagged groups=["friendly"].
    get_nodes_in_group("friendly") returns any node in that group regardless
    of type, so that orphaned, invisible-in-the-editor CollisionShape2D
    (sitting right at her position) was a fully valid attack target.
  Fix: removed the stray "friendly" group tag from defense_zone.
  Note: guard.tscn has the identical vestigial defense_zone/"friendly" tag,
    but it's harmless there since guard.gd already adds the guard itself to
    "friendly" independently. Left alone — flag if any future NPC is cloned
    from guard.tscn/tenchijin.tscn and needs to NOT be a valid monster
    target, since this same trap will reappear.

BUG FIX: Dialogic addon failed to compile after the Godot 4.6 → 4.7 upgrade
  Files: addons/dialogic/Modules/Text/node_name_label.gd,
         addons/dialogic/Modules/Variable/subsystem_variables.gd
  Problem: three _get()/_set() property overrides (bundled Dialogic addon
    code, not ours) didn't return a value on every code path — e.g. the
    "property not found" fallthrough had no explicit return. Godot 4.6
    allowed this; 4.7's GDScript compiler enforces it, so the upgrade
    surfaced it as "Parse Error: Not all code paths return a value",
    cascading into "failed to compile depended scripts" across most of
    Dialogic's Text/Variable modules — almost certainly also breaking
    cutscenes, not just spamming the console.
  Fix: added explicit `return false` / `return null` on the missing paths
    in node_name_label.gd:_set() and both subsystem_variables.gd _get()
    overrides (module-level and the nested VariableFolder class).

BALANCE: Coin drop chance increased
  File: Scripts/enemy_spawner.gd
  coin_drop_chance 0.3 → 0.5 — drops felt too sparse in this session's
  playtesting. coin_drop_amount_range (1-4 per drop) unchanged.

--------------------------------------------------------------------------------
SESSION: 2026-08-26
--------------------------------------------------------------------------------

FEATURE: Lumora Outskirts — first endless-mode level
  Files: Scenes/lumora_outskirts.tscn (new), Scripts/main.gd
  Built as a new level (ground/walls/objects TileMapLayers under a "scenery"
  Node2D) started from a clone of main.tscn. Several things needed fixing
  before it was actually playable:
  - Draw order: ground/walls/objects were stacked in the wrong sibling order
    (ground drawn last = on top, hiding the other two). Fixed with explicit
    z_index (ground=0, walls=1, objects=2) instead of relying on tree order.
  - Wall collision: the walls TileSet had a Physics Layer configured
    (collision_layer=16/Environment, mask=111) but zero tiles actually had a
    collision polygon painted — walls were fully walkable. Painted a
    full-tile collision box onto all 17 distinct wall tile types (confirmed
    no overlap with the objects layer's tile IDs, which shares the same
    TileSet resource with walls — objects should stay unpainted on the
    physics layer to avoid accidentally gaining collision).
  - Boundary walls / camera limits: main.gd._setup_map_bounds() only looked
    for a direct child literally named "grass" (main.tscn's layout). This
    level's ground layer is scenery/ground, so zero boundary walls were ever
    created. Added a fallback lookup (scenery/ground) so both layouts work.
  - Guards not colliding with walls: guard.tscn's collision_mask (586)
    didn't include the Environment layer bit even once walls got real
    collision. Fixed 586 → 602 (adds bit 16). Monsters' mask already
    included it — no change needed there.
  - Enemies frozen in place: the level had no village_center/extraction_points
    group markers, which monsters.gd's waypoint state machine requires —
    confirmed via a live run that every spawned monster logged "No
    village_center found" and never moved. Added village_center + two
    extraction_point Marker2Ds (placeholder positions near the guards —
    reposition to taste). Also had no NavigationRegion2D at all (main.tscn
    has a hand-baked one); without one, monsters.gd's pathing code zeroes
    velocity every frame even with a valid waypoint target. Added a simple
    rectangular NavigationPolygon covering the playable area so monsters
    move — it doesn't yet know about the interior walls, so pathing is
    straight-line plus physics deflection rather than smart routing; a
    proper bake excluding wall footprints is a good follow-up once the wall
    layout is finalized.

FEATURE: Woodstalker — first endless-mode player class
  Files: Scenes/woodstalker.tscn (new), Scripts/woodstalker.gd (new)
  Duplicated from player.tscn/player.gd rather than subclassed, since the
  two are expected to diverge over time (balance, eventually distinct art).
  player.gd was already written *as* the Woodstalker (spells_unlocked dict,
  SPELL_PROGRESSION array) with a comment anticipating this exact split
  ("Story campaign: all pre-unlocked. Endless mode: use ability_chest drops
  to unlock in order") — woodstalker.gd just finishes that: spells_unlocked
  now starts all false, so the existing _unlock_next_spell()/ability_chest
  machinery grants them in SPELL_PROGRESSION order as chests drop. Root node
  renamed "woodstalker" but kept groups=["player"] so monster targeting and
  other group-based lookups are unaffected. player.tscn/player.gd untouched.

FEATURE: Ability chest — real sprite + collision, wired into monster drops
  Files: Scenes/chest.tscn, Scripts/ability_chest.gd, Scripts/monsters.gd
  chest.tscn's root was a Node2D with its CollisionShape2D nested under the
  Sprite2D — inert, since a CollisionShape2D only functions as a direct
  child of a CollisionObject2D. Changed root to Area2D (with ability_chest.gd
  attached) and moved CollisionShape2D to be its direct child. Removed the
  placeholder Polygon2D body/lid + code-built CollisionShape2D from
  ability_chest.gd's _ready() now that the real scene provides them.
  monsters.gd._spawn_ability_chest() now instantiates chest.tscn instead of
  building a bare Area2D from scratch.

FEATURE: Main menu — endless mode flow (level select, class select, credits)
  Files: Scripts/global.gd, Scripts/main_menu.gd, Scripts/victory_scene.gd,
         Scripts/main.gd, Scripts/camera_controller.gd,
         Scenes/endless_level_select.tscn (new), Scripts/endless_level_select.gd (new),
         Scenes/class_select.tscn (new), Scripts/class_select.gd (new),
         Scenes/lumora_outskirts.tscn
  - Global.campaign_complete now persists to user://saves/progress.json
    (load_progress()/save_progress(), mirroring the existing high_scores
    pattern). victory_scene.gd sets it true and saves on a real campaign win.
  - main_menu.gd: endless_button/credits_button are disabled (and removed
    from keyboard/gamepad focus via focus_mode) until campaign_complete is
    true. Wired to endless_level_select.tscn and victory_scene.tscn
    (credits mode) respectively. start_button/skip_button now reset
    Global.is_endless_mode = false so a stale flag can't leak into a fresh
    campaign run.
  - endless_level_select.tscn/.gd: level picker (Lumora Outskirts is the one
    entry so far) → sets Global.selected_endless_level, goes to class select.
  - class_select.tscn/.gd: class picker (Woodstalker is the one entry so
    far) → sets Global.selected_class_scene, loads the chosen level.
  - victory_scene.gd: added Global.viewing_credits branch — skips the
    eclipse/boss/stats preamble and the pause/UI-hide side effects, shows
    just the developer thank-you message with a single "Back to Menu"
    button instead of "The Wall of Heroes"/"Quit".
  - main.gd._apply_endless_class(): when Global.is_endless_mode, swaps in
    Global.selected_class_scene as the "player" node (same name, so
    name/group-based references elsewhere keep working). Spawns at the old
    player's position if one exists in the scene, else at a PlayerSpawn
    Marker2D, else the origin. Also reassigns Camera2D.target and
    enemy_spawner.player after the swap, since both cache "player" via
    @onready before this runs.
  - camera_controller.gd: target lookup changed from a hard `$"../player"`
    to get_node_or_null() + a null-guard in _process(), since the endless
    swap means "player" may not exist yet at Camera2D's own _ready() time.
  - lumora_outskirts.tscn: added a PlayerSpawn Marker2D (near the guards)
    since this level currently has no static player node at all.

COSMETIC: UI font color changed to red; villager stats hidden in endless mode
  Files: Scenes/ui.tscn, Scripts/ui.gd, Scripts/global.gd
  Added theme_override_colors/font_color = Color(1,0,0,1) to all 15 text
  nodes in ui.tscn (Labels + Resume/Quit buttons) — default white text was
  washing out against Lumora Outskirts' background. Added
  Global.is_endless_mode (defaults false) and ui.gd now hides the
  saved/lost/remaining-villagers labels when it's true, since endless mode
  has no villager-rescue objective.

BUG FIX: No monsters ever spawned in endless mode
  File: Scripts/main.gd
  Problem: enemy_spawner.gd's _ready() disables its own processing
  (set_process_mode(PROCESS_MODE_DISABLED)) whenever it can't find a player
  at that moment — and in endless mode it never can, since the spawner's own
  _ready() runs before main.gd's player-class swap (children ready before
  parents). PROCESS_MODE_DISABLED also stops the spawner's spawn_timer/
  wave_timer children from ever ticking, so nothing ever spawned regardless
  of spawner config, spawn points, or nav bake.
  Fix: main.gd._apply_endless_class() now sets
  spawner.process_mode = Node.PROCESS_MODE_INHERIT right after handing it
  the real player reference. Verified live: monsters spawning within seconds.

BUG FIX: Villager saved/lost/remaining labels still showing in endless mode
  File: Scripts/ui.gd
  Problem: the hide-labels-in-endless-mode logic lived in ui.gd's _ready(),
  but UI is itself an autoload — _ready() only runs once at game boot,
  long before the player has chosen endless mode.
  Fix: moved the check into _on_node_added()'s existing
  `if node.name == "main"` branch, which re-fires every time a level scene
  loads, so it now reads Global.is_endless_mode at the right moment.
  Verified via a full main_menu → endless → level run (not just the level
  loaded standalone, which doesn't reproduce the timing bug).

FEATURE: Enemies chase the player directly in endless mode
  File: Scripts/monsters.gd
  Problem: monsters' only movement AI is the story campaign's village-
  defense rally system — path to a village_center Marker2D, then to the
  nearest extraction_points Marker2D, then stop and fight whoever's nearby.
  They never actually walk toward the player. In Lumora Outskirts this read
  as every monster marching toward village_center (positioned east of most
  spawn points) regardless of where the player stood.
  Fix: added _physics_process_chase_player(), used only when
  Global.is_endless_mode — continuously retargets
  navigation_agent.target_position at the player's live position every
  frame using the same NavigationAgent2D/avoidance pipeline. _update_waypoint()
  now no-ops in endless mode so the two systems don't fight each other.
  Story campaign behavior is untouched.

FEATURE: Persistent endless-mode currency
  Files: Scripts/global.gd, Scripts/woodstalker.gd, Scripts/pet.gd
  Added Global.endless_coins, separate from the story campaign's per-run
  coins_collected/Solari — persists to user://saves/progress.json alongside
  campaign_complete via the existing load_progress()/save_progress().
  add_endless_coins(n) / spend_endless_coins(n) -> bool (checks funds,
  deducts, saves, returns success) are ready for the main-menu upgrade store
  once that's built. Wired into the two places coins are actually collected
  in endless mode (woodstalker.gd and pet.gd's pickup handlers), both
  guarded by Global.is_endless_mode.

BUG FIX: Woodstalker's Wolf pet appeared immediately in endless mode
  File: Scripts/woodstalker.gd
  Problem: _ready() unconditionally called _spawn_pet(1) — copied verbatim
  from player.gd, where it's correct (story mode pre-unlocks call_pet). In
  endless mode call_pet is meant to be chest-gated like every other spell.
  Fix: guarded the call with
  `if spells_unlocked.get("call_pet", false): call_deferred("_spawn_pet", 1)`.

FEATURE: Menu background music no longer restarts between menu screens
  Files: Scripts/global.gd, Scripts/main_menu.gd, Scripts/endless_level_select.gd,
         Scripts/class_select.gd, Scenes/main_menu.tscn,
         Scenes/endless_level_select.tscn, Scenes/class_select.tscn
  Problem: each of the three menu scenes had its own autoplaying
  background_music AudioStreamPlayer node — since each scene change frees
  the previous scene, the track restarted from 0 every time the player
  moved between main menu / level select / class select.
  Fix: moved playback to a persistent AudioStreamPlayer on the Global
  autoload (Global.play_menu_music() / stop_menu_music(), idempotent — a
  second play_menu_music() call while already playing is a no-op). Removed
  the redundant background_music nodes from all three scenes; each scene's
  _ready() just calls Global.play_menu_music() now. Music only stops when
  actually leaving the menu flow (campaign start/skip, credits, entering an
  endless level).

FEATURE: Objects layer made collidable (Lumora Outskirts)
  File: Scenes/lumora_outskirts.tscn
  Painted the same full-tile collision box onto all 17 distinct tile types
  used on the objects layer that walls already had (same physics layer —
  Environment/layer 5, mask 111 — since objects and walls share one TileSet
  resource). Confirmed zero overlap with the wall tile IDs beforehand.

FEATURE: Troubadour, Arcanist, Voidknight, and Gravecaller — four more
    endless-mode playable classes
  Files: Scripts/troubadour.gd, Scenes/troubadour.tscn (new);
         Scripts/arcanist.gd, Scenes/arcanist.tscn (new);
         Scripts/voidknight_class.gd, Scenes/voidknight_class.tscn (new);
         Scripts/gravecaller_class.gd, Scenes/gravecaller_class.tscn (new);
         Scripts/ally_minion.gd (new);
         Scenes/class_select.tscn, Scripts/class_select.gd
  Troubadour and Arcanist are converted from the existing story-campaign
  allies (annadaeus.gd/tenchijin.gd stay untouched) — same shape as the
  Woodstalker conversion: AI movement/auto-targeting replaced with player
  input, their real ability code (auras, Finale, Illusory Double / Frost
  Nova, Meteor, Disintegrate, Time Warp) kept and gated behind
  spells_unlocked + SPELL_PROGRESSION, unlocked one per ability_chest like
  every other class.
  Voidknight and Gravecaller are a bigger conversion since the elite-enemy
  versions (voidknight.gd/gravecaller.gd, also left untouched — still used
  by enemy_spawner.gd's boss_configs) extend monsters.gd, not CharacterBody2D
  at all — built fresh player-shaped scripts from scratch and ported their
  unique abilities over. The more interesting part: as enemies, all their
  abilities targeted the player's side (Necrotic Grasp slowed the player,
  Void Slash/Plague Cloud hit player/friendly, their permanent minions
  attacked the player). All of that had to be flipped to target "monsters"
  now that they're on the player's side. Their permanent minions
  (Skeleton/Wraith) previously used enemy_pet.gd, which is hardcoded to
  attack player/friendly/villagers — added ally_minion.gd as a friendly-
  targeting mirror of the same movement/respawn/attack pattern.
  All four verified live via the real menu flow (main menu → endless →
  class select → level), each with every ability force-unlocked (and
  minions explicitly spawned) for a full run — zero script errors.
  Found but did not fix (out of scope — enemy-side, not part of this
  conversion): gravecaller.tscn's bullet_pool node_scene points at an empty
  undefined PackedScene sub-resource instead of death_bolt.tscn, so the
  *enemy* Gravecaller's Death Bolt is very likely firing broken/invisible
  projectiles right now. Worth a look separately. (User fixed this directly
  in gravecaller.tscn afterward.)

BUG FIX: Voidknight/Gravecaller player projectiles weren't colliding with monsters
  Files: Scenes/voidknight_class.tscn, Scenes/gravecaller_class.tscn,
         Scenes/life_siphon_player.tscn (new), Scenes/death_bolt_player.tscn (new)
  Problem: their open_fire() set bolt.owner_group = "player" (a logic-only
  string used for scoring/attribution), but Life Siphon/Death Bolt still
  reused the elite-enemy projectile scenes as-is, whose collision_layer/mask
  were configured for an enemy hitting the player — never updated for a
  player hitting monsters.
  Fix (user): duplicated both into life_siphon_player.tscn/death_bolt_player.tscn
  with collision reconfigured to hit monsters, added to the "player" group,
  and repointed each class's bullet_pool.node_scene at the new scenes.

BUG FIX: Voidknight/Gravecaller basic-attack damage edits were being silently
    overwritten
  Files: Scripts/voidknight_class.gd, Scripts/gravecaller_class.gd
  Problem: the user resized Life Siphon and set both projectiles' damage to
  4 (to match the arrow baseline) directly on the new _player projectile
  scenes — but both classes' open_fire() unconditionally runs
  `bolt.damage = int(base_damage * damage_modifier)` every shot (same
  pattern every other class uses so potion damage-boosts keep working),
  which instantly overwrote the scene-level edit back to the old
  enemy-derived base_damage (20 / 28).
  Fix: changed base_damage's export default to 4 in both scripts instead of
  editing the projectile scenes — verified with an actual fired shot
  (bolt.damage == 4) rather than just reading the math.

================================================================================
